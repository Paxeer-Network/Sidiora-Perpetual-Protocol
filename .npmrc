# Perpetual Product Market Maker
# Package manager configuration

# Use exact versions by default
save-exact=true

# Hoist dependencies for Hardhat plugin compatibility
shamefully-hoist=true

# Disable update notifier noise
update-notifier=false

# Engine strict mode
engine-strict=true
